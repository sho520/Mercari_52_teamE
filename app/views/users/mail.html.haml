= javascript_include_tag 'jquery.jpostal.js'
- breadcrumb :identification
%body
  %nav.pankuzu
  .mypage__container
    .nav
      = render 'mypage_nav'
    %main.registration__main
      %section.registration__main__container
        %h2.registration__main__head メール/パスワード
        .registration__main__form
        = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
          .user__form__content
            .user__form__group
              %label{for: "user__title"} メールアドレス
              = f.email_field :email, class: 'user__form__input', placeholder: "例) sample@mailcom", type: "text", value: current_user.email
            %p.user__form__infomation__b.font__size
              メールアドレスを変更すると確認メールが送信されます。メール内のURLをクリックすると変更完了です。
            .user__form__group
              %label{for: "user__title"} 現在のパスワード
              = f.password_field :current_password, class: 'user__form__input', placeholder: "6文字以上", autocomplete: "current-password"
            .user__form__group
              %label{for: "user__title"} 現在のパスワード
              = f.password_field :password, class: 'user__form__input', placeholder: "6文字以上", autocomplete: "new-password"
            .user__form__group
              %label{for: "user__title"} 現在のパスワード
              = f.password_field :password_confirmation, class: 'user__form__input', placeholder: "6文字以上", autocomplete: "new-password"
            %p.user__form__infomation__b.font__size
              パスワードを設定したい場合は上記をすべて入力してください。（設定しない場合は空欄）
            = f.button :"変更する", class: 'user__form__botton'
            %br
            %br

